# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to manage versioning and changelogs for packages.

## Adding a changeset

Run `pnpm changeset` to create a new changeset.

## Publishing

Run `pnpm version-packages` to consume changesets and bump versions, then run `pnpm release` to publish to npm.
